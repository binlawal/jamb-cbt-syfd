# Production Environment Variables (Render)
# Copy these to your Render dashboard

# Backend Environment Variables
NODE_ENV=production
PORT=3000
DATABASE_URL=postgresql://user:password@host:5432/database  # Get from Render PostgreSQL
REDIS_HOST=  # Leave empty for in-memory cache
JWT_SECRET=your-super-secret-jwt-key-here  # Generate with: openssl rand -base64 32
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here  # Generate with: openssl rand -base64 32
CORS_ORIGIN=https://your-app.vercel.app  # Your Vercel frontend URL

# Frontend Environment Variables (Vercel)
VITE_API_URL=https://your-backend.onrender.com  # Your Render backend URL
